apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-tweeter-app.fullname" . }}
  labels:
{{ include "python-tweeter-app.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "python-tweeter-app.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
